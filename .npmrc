# 官方 seam 包的 peer 闭包里包含一个未发布到 npm 的包
# （@deepseek-ai/dsh-type-meta），npm 的 peer 自动安装会因为 404 直接中断整个
# install。跳过 peer 自动安装即可——类型层面由 skipLibCheck 兜住，运行时的
# 真实实例由宿主 dsh 提供。
#
# 这是环境约束，不是偏好设置。上游一旦补发该包，本文件即可删除。
legacy-peer-deps=true
